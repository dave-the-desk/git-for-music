{
  "name": "@git-for-music/db",
  "version": "0.0.1",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "generate": "prisma generate",
    "db:push": "set -a; . ../../.env; set +a; prisma db push",
    "migrate": "set -a; . ../../.env; set +a; prisma migrate dev",
    "studio": "set -a; . ../../.env; set +a; prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^6.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "prisma": "^6.0.0",
    "typescript": "^5.0.0"
  }
}
