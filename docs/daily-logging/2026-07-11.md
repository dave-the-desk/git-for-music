# 2026-07-11

## Implementation Log

- Fixed manual version checkout so the active-version row now moves to the selected node instead of sticking to the previous branch, and restored head-following updates when the checked-out branch advances.
- Changed the version-history checkout affordance to request head-following on branch switches, so the graph and workspace stay aligned after a manual move.
- Added regressions for explicit active-version updates on existing rows, branch-following reducer behavior, and the checkout click path in the demo client.
- Stopped the DAW client from forcing everyone onto a changing branch head by
  syncing the visible selection to the user's own active checkout instead of
  the shared branch head.
- Preserved branch-local author attribution and active-version checkout state
  through the snapshot, reducer, and per-user active-version resolution paths,
  so remote branch activity no longer rewrites other users' navigation state.
- Added regressions for the client branch-follow bug, the per-user active
  checkout resolver, the snapshot creator hydration path, and the version-tree
  creator lookup.
- Collapsed the docked version-history rail into a single predictable scroll
  path by removing the extra outer scroll wrappers around the tree.
- Added a rail-height-driven reset signal so the version-history tree resets
  again after the docked rail gets its measured height, not just on the first
  mount.
- Updated the tree and docked-rail interaction tests to cover the new reset
  signal and confirm the rail height feeds through to the tree props.
- Added zoom controls to the demo Version history rail header so users can zoom
  in, zoom out, and reset from the top-right control cluster.
- Kept the zoom state local to the history tree and applied it as a graph
  scale, so the version graph layout stays unchanged while the viewport zooms.
- Added interaction coverage for the zoom controls from the rail header.
- Moved the zoom controls into the top-right of the Version history rail, removed
  the shortcut hint text, and let zoom-out continue below the old 75% floor.
- Reworked the version-history graph layout so sibling branches share the same
  row, spread horizontally, and keep their parent centered above the full
  sibling span.
- Reworked the version-history palette so the active branch renders in blue,
  the active checkout node uses a lighter blue, off-branch nodes use a lighter
  yellow, and off-branch heads use a darker yellow.
- Hydrated version-history actor labels from resolved user display names in the
  snapshot state so the tree shows account names instead of raw actor IDs or
  unknown-user fallbacks.
- Persisted resolved creator names on version nodes so non-root entries render
  the creator's display name instead of falling back to `Unknown user`.
- Changed the root version-history node summary to read `[demo name] created`
  instead of the generic `Initial version` text.
- Stamped creator display names into every version-creation payload and
  propagated them through the snapshot and reducer path so newly created nodes
  render the author's name on first paint instead of waiting for later
  hydration.
- Switched the inspector measurement for the version-history rail to a
  layout-phase update so the rail mounts at its final height instead of
  flashing tall and then shrinking a frame later.
- Skipped the version-history rail auto-scroll on the initial mount so the tree
  opens at the top instead of jumping into view at the start.
- Removed the version-history rail auto-scroll path entirely so the page no
  longer jumps to the tree on load or when the tree tab becomes active.
- Forced the version-history scroll container back to `scrollTop = 0` on mount
  so the tree opens at the top even if the browser restores a nested scroll
  position.
- Removed the redundant rename button from the version-details popup and made
  the title itself open inline rename mode.
- Hid the "Go back to this version" action when the inspected node is already
  the selected checkout version.
- Removed the version ID and parent metadata rows from the version-details popup.
- Derived the branch field from the original branch source node so it stays in
  sync when that source node is renamed.
- Made the Operation field fall back to the matching history entry when the
  version snapshot does not carry an `operationSeq`.
- Removed the Operation field from the version-details popup entirely.
- Audited `docs/` for source-contradicting stale claims and clarified or
  corrected the affected notes.
- Refreshed generated inventories from the current `rg --files` output so route,
  package, migration, plugin, and test paths match the repo.
- Updated protocol/test wording from generic Jest-only guidance to the current
  Vitest plus `tsx --test` split.
- Marked resolved finding notes as historical for the version-history initial
  scroll bug and plugin module CSP sandbox load bug.
- Updated plugin branch docs to reflect the shipped WAM insert chain, plugin
  library/upload/grant/module-proxy implementation, and remaining plugin UI
  polish.
- Removed dead `realtime-collaboration-plan.md` references and aligned the
  realtime/versioning notes with the current transform/rebase source.
- Expanded the root README into a GitHub-facing onboarding guide that explains
  the Docker-backed local database/services, `pnpm db:generate` -> `pnpm db:push`
  -> `pnpm dev` workflow, repo layout, docs/Obsidian preference, test harnesses,
  routes, architecture notes, and troubleshooting.
- Clarified in the README that the project uses the MIT License and summarized
  the practical permission and warranty implications.
- Fixed branch creation so shared version-tree events no longer move every
  viewer onto the new head; the creator still updates through the explicit
  active-version response path.
- Stopped version creator metadata from being rewritten after creation by
  preserving existing `createdBy` / `createdByName` values and hydrating creator
  names from the earliest matching creation record.
- Updated the version-history tree fallback to use the original creation event
  and added regressions for the server snapshot hydrate path, reducer behavior,
  project sync behavior, and tree rendering.
- Tightened the per-user active-version resolver so an existing checkout row is
  preserved on reload instead of being promoted to a newer sibling branch just
  because the demo head changed elsewhere.
- Updated the realtime rebootstrap tests so tree refreshes preserve the viewer's
  checkout while still reflecting the new branch in shared version history.

## Verification

- `pnpm exec tsx --test app/lib/daw/server/demo-user-active-version.test.ts app/lib/daw/server/command-api.test.ts` from `packages/server/`
- `pnpm --filter @git-for-music/web test -- src/app/lib/daw/state/operation-reducer.test.ts src/app/pages/groups/demo/components/daw/DemoDawClient.interaction.test.tsx src/app/pages/groups/demo/components/daw/VersionHistoryTree.interaction.test.tsx`
- `pnpm --filter @git-for-music/web test -- src/app/pages/groups/demo/components/daw/VersionHistoryTree.interaction.test.tsx src/app/pages/groups/demo/components/daw/DemoDawClient.interaction.test.tsx`
- `pnpm --filter @git-for-music/web test -- src/app/pages/groups/demo/components/daw/VersionHistoryTree.interaction.test.tsx src/app/lib/daw/state/operation-reducer.test.ts`
- `pnpm --filter @git-for-music/web test -- VersionHistoryTree.interaction.test.tsx`
- `pnpm exec tsx --test app/lib/daw/server/versioning.test.ts app/lib/daw/server/commands/revert-version.test.ts app/lib/daw/server/commands/upload-track.test.ts`
- `pnpm --filter @git-for-music/web test -- src/app/lib/daw/state/operation-reducer.test.ts src/app/lib/daw/engine/project-sync-engine.test.ts src/app/pages/groups/demo/components/daw/VersionHistoryTree.interaction.test.tsx`
- `pnpm exec tsx --test app/lib/daw/server/snapshot-builder.test.ts`
- `pnpm --filter @git-for-music/web test -- src/app/lib/daw/engine/project-sync-engine.test.ts`
- `pnpm exec tsx --test app/lib/daw/server/demo-user-active-version.test.ts`
- `pnpm --filter @git-for-music/web test -- src/app/pages/groups/demo/components/daw/DemoDawClient.interaction.test.tsx`
- `python3` markdown-link check across `docs/**/*.md` (only intentional
  template placeholder links remain unresolved).
- `python3` wikilink check across `docs/**/*.md` using Obsidian-style filename
  resolution.
- `rg` stale-claim scans for removed realtime-plan links, obsolete plugin
  implementation gaps, and stale test guidance.
- Regenerated inventories from `rg --files src/app`, `rg --files packages/server`,
  `rg --files packages/db`, and `rg --files packages/shared`.
- `git diff --check`
- Result: 7 files passed, 32 tests passed.
- Result: 12 server tests passed.
- Result: 33 interaction tests passed.
- Result: 35 interaction tests passed.
- Result: docs audit checks passed; no code test suites were run for the
  docs-only audit changes.
