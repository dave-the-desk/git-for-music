# Project page cleanup

- Removed the `Recent Activity` and `Equipment Requirements` placeholder
  sections from the project page client.
- Added interaction coverage to ensure those headings stay absent while the
  demos section still renders.

## Verification

- `pnpm --filter @git-for-music/web test -- src/app/pages/groups/project/project-page-client.interaction.test.tsx`

# DAW single-solo behavior

- Restricted DAW soloing so only one track can be soloed at a time.
- Updated the demo client to replace any existing solo selection instead of accumulating multiple solo tracks.
- Hardened the playback engine so an incoming project snapshot or live solo change normalizes to a single active solo track.
- Added regression coverage for the engine-level solo constraint and the UI interaction flow that swaps solo selection between tracks.

## Verification

- `pnpm --filter @git-for-music/web test -- src/app/lib/daw/engine/playback-engine.test.ts src/app/pages/groups/demo/components/daw/DemoDawClient.interaction.test.tsx`

# 2026-07-15

## Version-history first-frame viewport

- Traced the initial full-height graph followed by visible shrinking to a
  `ResizeObserver` feedback loop between the version-history rail and the
  inspector in their shared auto-sized grid row.
- Confirmed the graph is a custom DOM/SVG visualization with deterministic
  `buildGraphLayout` coordinates, CSS scale zoom, and native scrolling; no
  graph-library fit or animated viewport API participates in initialization.
- Removed the inspector measurement state, post-render inline height, observer,
  and arbitrary `+45px` adjustment.
- Added desktop CSS size containment so graph intrinsic height cannot size the
  shared grid row, plus a fixed reduced viewport for the single-column layout.
  The final viewport now exists in the browser's first layout and has no later
  initialization write that could animate or converge.
- Scoped graph scroll reset to demo identity and rail expansion state, and
  added regression coverage proving live version additions preserve a user's
  viewport while demo changes still have an identity-specific reset signal.
- Recorded the full diagnosis and production-browser frame sampling in
  [Version-history tree initial viewport findings](../60_BRANCHES/daw-realtime-collab/version-history-tree-initial-scroll-findings.md#2026-07-15-follow-up-initial-full-height-graph-then-shrinking-viewport).

### Version-history viewport verification

- `pnpm --filter @git-for-music/web exec vitest run app/pages/groups/demo/components/daw/DemoDawClient.interaction.test.tsx app/pages/groups/demo/components/daw/VersionHistoryTree.interaction.test.tsx` (29 passed)
- `pnpm --filter @git-for-music/web test` (43 passed)
- `pnpm --filter @git-for-music/web test:unit` (133 passed, 3 skipped)
- `pnpm --filter @git-for-music/web typecheck`
- `pnpm lint` (passes with pre-existing warnings only)
- `pnpm --filter @git-for-music/web build` (passes with pre-existing WAM dynamic-import and lint warnings)
- Production Chromium frame sampling covered small and large hard navigations,
  client-side navigation, switching demos, responsive resizing, rail
  collapse/expand, and user zoom. Each initial graph exposure produced one
  stable viewport height from its first visible sampled frame.

## DAW track identity after rename

- Fixed the rename-then-add-track regression where duplicate placeholder
  cleanup could collapse distinct tracks by treating mutable `trackName` as an
  identity key.
- Changed client and server blank-track cleanup to reconcile only matching
  `trackId` values. Same-name tracks with distinct IDs are now preserved.
- Updated default `Track N` naming to consider stable lane position as well as
  generated labels, so renaming the first lane no longer makes the next add
  reuse `Track 1`.
- Added reducer coverage that replays rename, branch creation, and track
  creation, then verifies the renamed ID remains and the new ID is distinct.
- Strengthened rename, client/server cleanup, naming, and upload branch-builder
  tests to cover the identity invariant.
- Recorded the verified cause, corrective behavior, affected source, and
  regression evidence in
  [Session Problems and Solutions](../60_BRANCHES/daw-realtime-collab/session-problems-and-solutions.md#6-renaming-a-track-could-make-a-later-track-replace-it).
- Follow-up reproduction showed the identity cleanup corrected the new label to
  `Track 2` but did not stop replacement. Read-only database inspection found a
  populated branch followed by an empty automatic checkpoint, then a second
  branch containing only the newly added track.
- Fixed `createAutoDemoVersion` so automatic and semantic checkpoints clone the
  active source tracks instead of creating zero-track nodes. Logical `trackId`
  values survive the clone while immutable `trackVersionId` values remain new.
- Automatic checkpoint nodes are now recorded and broadcast as durable
  `VERSION_NODE_ADDED` operations with their parent and cloned track payload,
  preventing snapshot-tail bootstrap from rendering the next branch without
  its parent.
- Added regressions for the populated checkpoint-to-next-branch chain and the
  durable connected-node operation payload.
- Added current-bootstrap compatibility reconciliation for automatic nodes that
  were already persisted by the older event-only implementation but absent from
  snapshot operation tails. Historical sequence reads remain unchanged.
- Added the reusable
  [DAW Version-State Debugging Protocol](../01_PROTOCOLS/daw-version-state-debugging.md),
  covering identifier invariants, read-only database timelines, layered replay
  tracing, regression design, confidence calibration, and documentation handoff.

## Verification

- `pnpm --filter @git-for-music/web exec node --import tsx --test app/lib/daw/utils/track-duplicate-cleanup.test.ts app/lib/daw/utils/track-names.test.ts app/lib/daw/state/timeline-edit-transforms.test.ts app/lib/daw/state/operation-reducer.test.ts`
- `pnpm --filter @git-for-music/server exec node --import tsx --test app/lib/daw/server/track-duplicate-cleanup.test.ts app/lib/daw/server/commands/upload-track.test.ts app/lib/daw/server/versioning.test.ts app/lib/daw/server/command-api.test.ts app/lib/daw/server/snapshot-builder.test.ts`
- `pnpm --filter @git-for-music/web test:unit` (126 passed, 3 skipped)
- `pnpm --filter @git-for-music/web test` (40 passed)
- `pnpm --filter @git-for-music/server test` (78 passed, 1 skipped)
- `pnpm --filter @git-for-music/web typecheck`
- `pnpm --filter @git-for-music/server typecheck`
- `pnpm lint` (passes with pre-existing warnings only)
- `git diff --check`

## Cross-track audio clip movement

- Separated segment destination ownership from immutable audio origin with the
  nullable, backward-compatible `Segment.sourceTrackVersionId` field and a
  Prisma migration.
- Preserved source identity through cross-track moves, splits, version clones,
  snapshots, accepted-operation replay, offline optimism, and reconnect.
- Updated playback to load each clip's source buffer while routing it through
  the destination track's mute, solo, gain, pan, and effects graph.
- Added destination-aware legacy route handling, invalid-target rejection,
  source-and-destination conflict scopes, and correct same-track undo bounds.
- Added server mutation/conflict/snapshot/versioning and client
  reducer/rebase/sync/undo/playback regressions.
- Recorded the verified failure and fix in
  [Session Problems and Solutions](../60_BRANCHES/daw-realtime-collab/session-problems-and-solutions.md#7-moving-a-clip-to-another-track-changed-its-audio-source).

### Cross-track verification

- `pnpm typecheck`
- `pnpm lint` (passes with pre-existing warnings only)
- Server Jest suites: 82 passed, 1 skipped
- Web Jest suites: 129 passed, 3 skipped
- Web interaction suites: 42 passed
- Prisma client generation and schema validation

## Cross-track implicit clip follow-up

- Fixed the new-DAW path where unsplit implicit clips entered track-offset drag
  mode instead of clip movement.
- Added persistent explicit-lane lifecycle state so moving the only clip leaves
  the source lane empty after accepted replay, snapshots, cloning, and reload.
- Materialized implicit moves atomically with a server-generated stable segment
  ID and source-audio metadata.
- Switched timeline waveforms to the segment's source URL and MIME rather than
  the destination track placeholder.
- Added an explicit empty-track upload source type and repaired existing
  generated placeholder WAV classifications with narrow migrations.
- Added server mutation, reducer/transform, reload, and waveform-source
  regressions; focused verification passes with 42 server and 65 client tests.

### Follow-up verification

- `pnpm typecheck`
- Server: 83 passed, 1 skipped
- Web unit: 132 passed, 3 skipped
- Web interaction: 42 passed
- `pnpm lint` (passes with pre-existing warnings only)
- Prisma generation and five applied migrations
- `git diff --check`

## Public release v1.0.1

- Bumped the root and workspace package versions to `1.0.1`.
- Updated the README to identify `v1.0.1` as the current stable release.
- Prepared the public patch release around four merged changes: the
  rename-then-add-track identity fix, exclusive solo behavior, project-page
  cleanup, and cross-track clip movement.
- Verified the release commit with the full GitHub Actions workflow before
  publishing the `v1.0.1` tag and GitHub Release.
