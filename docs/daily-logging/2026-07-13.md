# 2026-07-13

## Implementation Log

- Planned the public/private repository split: this repo stays the public baseline; a private hosted-product repo will clone it with an `upstream` remote and merge from tagged releases.
- Wrote what is now `docs/architecture/public-repo-baseline.md`, covering core-vs-product boundaries, typed configuration, provider extension points (auth, storage, analytics, billing, branding), a runtime feature registry with `FEATURE_*` env flags, test/lint/typecheck baselines, GitHub Actions CI, and the `v1.0.0` release target.
- Wrote what is now `docs/architecture/public-repo-implementation-record.md` as the ordered phase history linked to the architecture note.
- Wrote what is now `docs/guides/downstream-private-repo-setup.md`, documenting the private-fork setup (dual remotes, push safeguard, update-pull flow, and allowed customization surface) for public-repo users.
- Added `docs/architecture/core-vs-product-boundaries.md` and linked the downstream setup guide from the README so the public/private split is discoverable from the repo entry point.
- Added root/package `test` and `typecheck` scripts for the current test split, plus package-local `test:unit` and `test` scripts for the Node test suites.
- Triaged and fixed the remaining state/layout test failures in `src/app/lib/daw/state/operation-reducer.ts` and `src/app/lib/daw/state/operation-reducer.test.ts`.
- Confirmed the full web unit suite passes with `pnpm --filter @git-for-music/web test:unit` after the reducer and fixture fixes.
