# 2026-07-04

## Implementation Log

- Read the repo context router, the vault entrypoints, the non-negotiable rules, the implementation guide, and the DAW sync notes before touching anything.
- Extracted and reviewed the three real-time collaboration papers in `docs/papers-on-sync/`.
- Compared the paper model against the current DAW implementation in `src/app/lib/daw/` and `packages/server/app/lib/daw/`.
- Confirmed the current repo uses snapshots, an operation log, optimistic local replay, presence, and conflict-based branching rather than a Jupiter-style transform layer.
- Added a new paper summary and gap-analysis note at `docs/papers-on-sync/README.md`.
- Added a phased realtime collaboration plan at `docs/papers-on-sync/realtime-collaboration-plan.md`.
- Reviewed the git docs, realtime-collab papers, and the DoltHub commit-graph write-up, then wrote a branch-scoped design plan for combining real-time editing, automatic version saving, non-destructive revert, and concurrent branches, with a commit-graph Tree tab layout.
- Added the branch section at `docs/60_BRANCHES/daw-realtime-collab/` (`README.md` + `realtime-versioning-and-tree.md`).
- Studied the current versioning/sync/Tree-tab implementation (`command-api.ts`, `snapshot-builder.ts`, `versioning.ts`, `create-version.ts`, `operation-reducer.ts`, `realtime-gateway.ts`, `version-tree-layout.ts`, `VersionHistoryTree.tsx`, Prisma schema) and wrote a grounded, phased implementation checklist at `docs/60_BRANCHES/daw-realtime-collab/implementation-checklist.md`.
- Added a `DemoVersionKind` enum to `packages/db/prisma/schema.prisma` and defaulted `DemoVersion.kind` to `EXPLICIT` to establish provenance for future commit-node shaping work.
- Added a nullable `operationSeq` column to `DemoVersion` and a matching Prisma migration at `packages/db/prisma/migrations/20260704120000_add_demo_version_kind_and_operation_seq/migration.sql` so the Tree tab can order versions without re-deriving from the log.
- Clarified the branch checklist’s merge-shape item to specify forward-compatible storage only: keep `parentId` canonical, add a `DemoVersionParent` join later, and defer merge behavior.
- Added the forward-compatible `DemoVersionParent` join table plus migration at `packages/db/prisma/migrations/20260704123000_add_demo_version_parent_join/migration.sql` with `versionId`, `parentId`, and `order`.
- Updated `packages/server/app/lib/daw/server/versioning.ts` so `DEMO_VERSION_FIELDS` and `serializeCreatedDemoVersionTreeNode` carry `kind`, `operationSeq`, and merge flags through the server version payload.
- Threaded `kind` through `createDemoVersionWithCopiedTracks` and the explicit/branch creation call sites in `create-version.ts`, `create-demo.ts`, `upload-track.ts`, `complete-upload.ts`, and `command-api.ts`.
- Extended the shared version tree branch type and the client `DawVersion` model with `kind` and `operationSeq`, and preserved `kind` during local version normalization.
- Added a DAG-integrity test in `packages/server/app/lib/daw/server/versioning.test.ts` that walks created versions to ensure every non-root parent resolves and no cycles are present.
- Installed `tsx` at the workspace root so TypeScript `node:test` files can run with `node --import tsx --test ...`.
- Validated `packages/server/app/lib/daw/server/versioning.test.ts` successfully with `node --import tsx --test packages/server/app/lib/daw/server/versioning.test.ts` after fixing the test fixture and the lazy snapshot import path.
- Added checkpoint policy constants beside `DEFAULT_SNAPSHOT_CHECKPOINT_TAIL` in `packages/server/app/lib/daw/server/snapshot-builder.ts` for the auto-version debounce window and accepted-operation count threshold.
- Added `shouldCreateAutoVersion(...)` in `packages/server/app/lib/daw/server/snapshot-builder.ts` to trigger on semantic boundaries, idle debounce, or operation-count threshold.
- Added `createAutoDemoVersion(...)` in `packages/server/app/lib/daw/server/versioning.ts` to snapshot the current head into a new `DemoVersion` with `kind = AUTO` or `SEMANTIC`, `parentId` set to the source head, and `operationSeq` copied through.
- Wired `maybeCreateAutoDemoVersionAfterAcceptedOperation(...)` into `commitDawProjectOperation` so accepted operations can create an auto-version after commit, using the policy gate and keeping snapshot replay checkpointing separate.
- Changed the auto-version creation path to metadata-only mode so it does not call `cloneTrackVersionsToDemoVersion` and never carries copied audio tracks.
- Added a realtime `version_created` event in the server gateway and client sync engine so auto-version creation reboots the Tree tab immediately after commit.
- Added a `DAW_ENABLE_ORDINARY_EDIT_HEAD_ADVANCE` gate around the per-edit branch path for `TRACK_RENAMED` and `SEGMENT_TRIMMED`, so the legacy fork-on-edit behavior stays available while the new head-advancing auto-checkpoint flow is rolled out.
- Ran `packages/server/app/lib/daw/server/snapshot-builder.test.ts` with `TSX_TSCONFIG_PATH=packages/server/tsconfig.json node --import tsx --test ...`; the new predicate tests passed along with the existing snapshot replay coverage.
- Ran `packages/server/app/lib/daw/server/versioning.test.ts` with `TSX_TSCONFIG_PATH=packages/server/tsconfig.json node --import tsx --test ...`; the new auto-version helper test passed along with the existing version cloning and DAG integrity coverage.
- Ran `packages/server/app/lib/daw/server/command-api.test.ts` with `TSX_TSCONFIG_PATH=packages/server/tsconfig.json node --import tsx --test ...`; the auto-version commit-path test passed along with the existing command-api helper coverage.
- Extended `packages/server/app/lib/daw/server/command-api.test.ts` with burst and operation-count checkpoint cases so only one auto-version is created after an idle gap, and K accepted ops still force a checkpoint.
- The auto-version helper coverage still asserts metadata-only output by checking that audio-copy steps are skipped during `createAutoDemoVersion(...)`.
- Ran `src/app/lib/daw/engine/project-sync-engine.test.ts` with `TSX_TSCONFIG_PATH=packages/server/tsconfig.json node --import tsx --test ...`; the new `version_created` refresh test passed along with the existing sync-engine coverage.
- Added branch-scoped docs notes that mark Automatic Version Saving as an accepted-operation post-commit checkpoint flow, not a realtime edit transport fork, and called out the "IMPORTANT" web-app rule directly in the versioning/tree design note.
- Updated the `docs/40_FEATURES` versioning, branching, and realtime model docs so the product-level guidance now matches the implemented Automatic Version Saving flow and the `version_created` refresh event.
- Attempted a package-level `tsc` check in `packages/server`, but the workspace already has a pre-existing re-export ambiguity in `app/lib/daw/server/index.ts` (`DawDatabaseClient` is exported twice), so that compile path does not currently pass.

## Branch Context

- [daw-realtime-collab branch notes](../60_BRANCHES/daw-realtime-collab/README.md)

## Verification

- Validated the Prisma schema with `pnpm --filter @git-for-music/db exec prisma validate --schema prisma/schema.prisma` using a placeholder `DATABASE_URL`.
- Installed `tsx` with `pnpm add -Dw tsx`.
- `TSX_TSCONFIG_PATH=packages/server/tsconfig.json node --import tsx --test packages/server/app/lib/daw/server/snapshot-builder.test.ts` passed.
- `TSX_TSCONFIG_PATH=packages/server/tsconfig.json node --import tsx --test packages/server/app/lib/daw/server/versioning.test.ts` passed.
- `TSX_TSCONFIG_PATH=packages/server/tsconfig.json node --import tsx --test packages/server/app/lib/daw/server/command-api.test.ts` passed.
- `TSX_TSCONFIG_PATH=packages/server/tsconfig.json node --import tsx --test src/app/lib/daw/engine/project-sync-engine.test.ts` passed.
- The auto-version policy, helper, and accepted-operation wiring are now in place; replay snapshot checkpointing remains separate.
