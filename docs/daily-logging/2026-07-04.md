# 2026-07-04

## Implementation Log

- Read the repo context router, the vault entrypoints, the non-negotiable rules, the implementation guide, and the DAW sync notes before touching anything.
- Extracted and reviewed the three real-time collaboration papers in `docs/papers-on-sync/`.
- Compared the paper model against the current DAW implementation in `src/app/lib/daw/` and `packages/server/app/lib/daw/`.
- Confirmed the current repo uses snapshots, an operation log, optimistic local replay, presence, and conflict-based branching rather than a Jupiter-style transform layer.
- Added a new paper summary and gap-analysis note at `docs/papers-on-sync/README.md`.
- Added a phased realtime collaboration plan at `docs/papers-on-sync/realtime-collaboration-plan.md`.
- Reviewed the git docs, realtime-collab papers, and the DoltHub commit-graph write-up, then wrote a branch-scoped design plan for combining real-time editing, automatic version saving, non-destructive revert, and concurrent branches, with a commit-graph Tree tab layout.
- Added the branch section at `docs/60_BRANCHES/daw-realtime-collab/` (`README.md` + `realtime-versioning-and-tree.md`).
- Studied the current versioning/sync/Tree-tab implementation (`command-api.ts`, `snapshot-builder.ts`, `versioning.ts`, `create-version.ts`, `operation-reducer.ts`, `realtime-gateway.ts`, `version-tree-layout.ts`, `VersionHistoryTree.tsx`, Prisma schema) and wrote a grounded, phased implementation checklist at `docs/60_BRANCHES/daw-realtime-collab/implementation-checklist.md`.
- Added a `DemoVersionKind` enum to `packages/db/prisma/schema.prisma` and defaulted `DemoVersion.kind` to `EXPLICIT` to establish provenance for future commit-node shaping work.
- Added a nullable `operationSeq` column to `DemoVersion` and a matching Prisma migration at `packages/db/prisma/migrations/20260704120000_add_demo_version_kind_and_operation_seq/migration.sql` so the Tree tab can order versions without re-deriving from the log.
- Clarified the branch checklist’s merge-shape item to specify forward-compatible storage only: keep `parentId` canonical, add a `DemoVersionParent` join later, and defer merge behavior.
- Added the forward-compatible `DemoVersionParent` join table plus migration at `packages/db/prisma/migrations/20260704123000_add_demo_version_parent_join/migration.sql` with `versionId`, `parentId`, and `order`.
- Updated `packages/server/app/lib/daw/server/versioning.ts` so `DEMO_VERSION_FIELDS` and `serializeCreatedDemoVersionTreeNode` carry `kind`, `operationSeq`, and merge flags through the server version payload.
- Threaded `kind` through `createDemoVersionWithCopiedTracks` and the explicit/branch creation call sites in `create-version.ts`, `create-demo.ts`, `upload-track.ts`, `complete-upload.ts`, and `command-api.ts`.
- Extended the shared version tree branch type and the client `DawVersion` model with `kind` and `operationSeq`, and preserved `kind` during local version normalization.
- Added a DAG-integrity test in `packages/server/app/lib/daw/server/versioning.test.ts` that walks created versions to ensure every non-root parent resolves and no cycles are present.
- Installed `tsx` at the workspace root so TypeScript `node:test` files can run with `node --import tsx --test ...`.
- Validated `packages/server/app/lib/daw/server/versioning.test.ts` successfully with `node --import tsx --test packages/server/app/lib/daw/server/versioning.test.ts` after fixing the test fixture and the lazy snapshot import path.
- Added checkpoint policy constants beside `DEFAULT_SNAPSHOT_CHECKPOINT_TAIL` in `packages/server/app/lib/daw/server/snapshot-builder.ts` for the auto-version debounce window and accepted-operation count threshold.
- Added `shouldCreateAutoVersion(...)` in `packages/server/app/lib/daw/server/snapshot-builder.ts` to trigger on semantic boundaries, idle debounce, or operation-count threshold.
- Added `createAutoDemoVersion(...)` in `packages/server/app/lib/daw/server/versioning.ts` to snapshot the current head into a new `DemoVersion` with `kind = AUTO` or `SEMANTIC`, `parentId` set to the source head, and `operationSeq` copied through.
- Wired `maybeCreateAutoDemoVersionAfterAcceptedOperation(...)` into `commitDawProjectOperation` so accepted operations can create an auto-version after commit, using the policy gate and keeping snapshot replay checkpointing separate.
- Changed the auto-version creation path to metadata-only mode so it does not call `cloneTrackVersionsToDemoVersion` and never carries copied audio tracks.
- Added a realtime `version_created` event in the server gateway and client sync engine so auto-version creation reboots the Tree tab immediately after commit.
- Added a `DAW_ENABLE_ORDINARY_EDIT_HEAD_ADVANCE` gate around the per-edit branch path for `TRACK_RENAMED` and `SEGMENT_TRIMMED`, so the legacy fork-on-edit behavior stays available while the new head-advancing auto-checkpoint flow is rolled out.
- Ran `packages/server/app/lib/daw/server/snapshot-builder.test.ts` with `TSX_TSCONFIG_PATH=packages/server/tsconfig.json node --import tsx --test ...`; the new predicate tests passed along with the existing snapshot replay coverage.
- Ran `packages/server/app/lib/daw/server/versioning.test.ts` with `TSX_TSCONFIG_PATH=packages/server/tsconfig.json node --import tsx --test ...`; the new auto-version helper test passed along with the existing version cloning and DAG integrity coverage.
- Ran `packages/server/app/lib/daw/server/command-api.test.ts` with `TSX_TSCONFIG_PATH=packages/server/tsconfig.json node --import tsx --test ...`; the auto-version commit-path test passed along with the existing command-api helper coverage.
- Extended `packages/server/app/lib/daw/server/command-api.test.ts` with burst and operation-count checkpoint cases so only one auto-version is created after an idle gap, and K accepted ops still force a checkpoint.
- The auto-version helper coverage still asserts metadata-only output by checking that audio-copy steps are skipped during `createAutoDemoVersion(...)`.
- Ran `src/app/lib/daw/engine/project-sync-engine.test.ts` with `TSX_TSCONFIG_PATH=packages/server/tsconfig.json node --import tsx --test ...`; the new `version_created` refresh test passed along with the existing sync-engine coverage.
- Added branch-scoped docs notes that mark Automatic Version Saving as an accepted-operation post-commit checkpoint flow, not a realtime edit transport fork, and called out the "IMPORTANT" web-app rule directly in the versioning/tree design note.
- Updated the `docs/40_FEATURES` versioning, branching, and realtime model docs so the product-level guidance now matches the implemented Automatic Version Saving flow and the `version_created` refresh event.
- Attempted a package-level `tsc` check in `packages/server`, but the workspace already has a pre-existing re-export ambiguity in `app/lib/daw/server/index.ts` (`DawDatabaseClient` is exported twice), so that compile path does not currently pass.

## Branch Context

- [daw-realtime-collab branch notes](../60_BRANCHES/daw-realtime-collab/README.md)

## Verification

- Validated the Prisma schema with `pnpm --filter @git-for-music/db exec prisma validate --schema prisma/schema.prisma` using a placeholder `DATABASE_URL`.
- Installed `tsx` with `pnpm add -Dw tsx`.
- `TSX_TSCONFIG_PATH=packages/server/tsconfig.json node --import tsx --test packages/server/app/lib/daw/server/snapshot-builder.test.ts` passed.
- `TSX_TSCONFIG_PATH=packages/server/tsconfig.json node --import tsx --test packages/server/app/lib/daw/server/versioning.test.ts` passed.
- `TSX_TSCONFIG_PATH=packages/server/tsconfig.json node --import tsx --test packages/server/app/lib/daw/server/command-api.test.ts` passed.
- `TSX_TSCONFIG_PATH=packages/server/tsconfig.json node --import tsx --test src/app/lib/daw/engine/project-sync-engine.test.ts` passed.
- The auto-version policy, helper, and accepted-operation wiring are now in place; replay snapshot checkpointing remains separate.
- Added `packages/server/app/lib/daw/server/commands/revert-version.ts` so revert-to-version now creates a new `DemoVersion` with `kind = REVERT`, `parentId` pinned to the current branch head, and cloned track state from the chosen ancestor.
- Added `packages/server/app/lib/daw/server/commands/revert-version.test.ts` to cover the happy path and the non-ancestor validation path.
- Verified the new server command and the existing versioning helpers with `pnpm exec tsx --test app/lib/daw/server/commands/revert-version.test.ts app/lib/daw/server/versioning.test.ts` from `packages/server`.
- Added `src/app/pages/api/versions/revert/index.ts` so the revert flow now has a Next API endpoint that mirrors the existing version-create route's auth pattern and forwards to the new server command.
- Added `RevertVersionRequest` to `packages/shared/src/index.ts` so the route has an explicit shared request shape.
- Refreshed the generated source inventory at `docs/20_GENERATED_INVENTORIES/src-app-inventory.md` after adding the new API route.
- Re-ran `pnpm exec tsx --test app/lib/daw/server/commands/revert-version.test.ts` from `packages/server`; the revert command tests still pass after the endpoint wiring.
- Re-ran `pnpm exec tsc -p tsconfig.json --noEmit --pretty false` from `packages/server`; the package still fails on the pre-existing `command-api.ts`/`command-api.test.ts` errors and the existing `DawDatabaseClient` re-export ambiguity, not on the new revert route.
- Updated revert logging so the server now records `VERSION_REVERTED_FROM` with a full version node payload, and taught the client reducer and snapshot replay to upsert that node while still handling legacy checkout-only logs.
- Added reducer coverage in `src/app/lib/daw/state/operation-reducer.test.ts` for `VERSION_REVERTED_FROM` creating a new current version node on the active head.
- Re-ran `pnpm exec tsx --test src/app/lib/daw/state/operation-reducer.test.ts` with `TSX_TSCONFIG_PATH=packages/server/tsconfig.json`; the reducer suite passed, including the new revert-node test.
- Re-ran `pnpm exec tsx --test app/lib/daw/server/commands/revert-version.test.ts` and `pnpm exec tsx --test src/app/lib/daw/state/operation-reducer.test.ts` after widening the revert payload metadata; both suites still pass.
- Adjusted `revertToVersionCommand(...)` so it targets the shared branch head for the revert, but preserves a pinned checkout when `isFollowingHead` is false.
- Added a pinned-checkout revert case to `packages/server/app/lib/daw/server/commands/revert-version.test.ts`.
- Re-ran `pnpm exec tsx --test app/lib/daw/server/commands/revert-version.test.ts` and `TSX_TSCONFIG_PATH=packages/server/tsconfig.json pnpm exec tsx --test src/app/lib/daw/state/operation-reducer.test.ts`; both suites passed after the checkout-handling change.
- Added `ProjectSyncEngine.revertToVersion(...)` so the client now posts to `/api/versions/revert`, updates the local checkout from the response, and refreshes the version tree after the accepted revert.
- Wired a `Revert to this version` action into `VersionHistoryTree.tsx` and passed the callback through `DemoDawClient.tsx`, reusing the existing tree/history surface for the revert flow.
- Added `ProjectSyncEngine` coverage for the revert API call and the follow-head checkout update.
- Re-ran `TSX_TSCONFIG_PATH=src/tsconfig.json pnpm exec tsx --test src/app/lib/daw/engine/project-sync-engine.test.ts`; the engine suite passed, including the new revert test.
- Re-ran `pnpm exec tsx --test app/lib/daw/server/commands/revert-version.test.ts` from `packages/server`; the server revert suite still passes.
- Re-ran `TSX_TSCONFIG_PATH=src/tsconfig.json pnpm exec tsx --test src/app/lib/daw/state/operation-reducer.test.ts`; the reducer suite still passes with the revert-node behavior.
- Added lightweight React test infra in `src/` with `vitest`, `jsdom`, `@testing-library/react`, `@testing-library/user-event`, a Vitest config, and a shared cleanup setup.
- Added `src/app/pages/groups/demo/components/daw/VersionHistoryTree.interaction.test.tsx` to exercise the revert action through the rendered tree UI.
- Added `pnpm --filter @git-for-music/web test` and `pnpm test:web` entry points for the new React test harness.
- Re-ran `pnpm --filter @git-for-music/web test`; the interaction suite passed.
- Re-ran `TSX_TSCONFIG_PATH=src/tsconfig.json pnpm exec tsx --test src/app/pages/groups/demo/components/daw/version-history-tree.test.ts`; the existing tree-layout test still passes.
- Re-ran `node --import tsx --test src/app/pages/groups/demo/components/daw/version-history-tree.test.ts`; the tree-layout suite passed, including the new multi-parent regression.
- Tightened revert coverage so the server test now asserts the new revert node carries the ancestor's track content, the engine test covers both follow-head and pinned checkout responses, and the reducer test checks older versions remain in the tree after a revert.
- Re-ran `pnpm exec tsx --test app/lib/daw/server/commands/revert-version.test.ts`, `TSX_TSCONFIG_PATH=src/tsconfig.json pnpm exec tsx --test src/app/lib/daw/engine/project-sync-engine.test.ts`, and `TSX_TSCONFIG_PATH=src/tsconfig.json pnpm exec tsx --test src/app/lib/daw/state/operation-reducer.test.ts`; all three suites passed.
- Reworked `src/app/pages/groups/demo/components/daw/version-tree-layout.ts` to build an explicit `childrenMap`, preserve the existing topological sort, and accept future multi-parent metadata through an optional `parentIds` shape.
- Added a regression test in `src/app/pages/groups/demo/components/daw/version-history-tree.test.ts` that verifies a merge-style version with two parents is attached under both parents in the tree builder output.
- Added `buildGraphLayout(...)` to `src/app/pages/groups/demo/components/daw/version-tree-layout.ts` so rows now come from topological order, columns are assigned with the head->root pass, and each column gets a stable color palette entry.
- Wired `VersionHistoryTree.tsx` to the new graph layout so edges inherit branch colors and each node shows a matching commit dot plus state-aware border/background.
- Added `src/app/pages/groups/demo/components/daw/version-graph-layout.test.ts` to cover the chain, fork, and merge column-assignment rules.
- Extended `src/app/pages/groups/demo/components/daw/version-graph-layout.test.ts` with a determinism check that compares two shuffled input orders for the same graph and asserts identical node and edge layouts.
- Verified the rendered Tree tab with `pnpm --dir src test -- src/app/pages/groups/demo/components/daw/VersionHistoryTree.interaction.test.tsx`.
- Verified the pure layout suites with `node --import tsx --test src/app/pages/groups/demo/components/daw/version-graph-layout.test.ts src/app/pages/groups/demo/components/daw/version-history-tree.test.ts`.
- Renamed the Tree tab prop to `liveVersions` and updated `DemoDawClient.tsx` to pass `projectSyncState.projectState?.versions` directly, so the graph renders from the live `LocalProjectState.versions` source instead of the history snapshot.
- Re-verified the Tree tab after the live-version wiring change with the same layout and Vitest interaction tests.
- Added `buildGraphEdgePath(...)` to `src/app/pages/groups/demo/components/daw/version-tree-layout.ts` and switched Tree tab edge rendering from straight SVG lines to orthogonal elbow paths so the commit graph reads more like a classic branch/merge lane diagram.
- Added a pure routing regression test for the elbow path shape in `src/app/pages/groups/demo/components/daw/version-graph-layout.test.ts`.
- Re-ran `node --import tsx --test src/app/pages/groups/demo/components/daw/version-graph-layout.test.ts src/app/pages/groups/demo/components/daw/version-history-tree.test.ts` and `pnpm --dir src test -- src/app/pages/groups/demo/components/daw/VersionHistoryTree.interaction.test.tsx`; both passed.
- Added `src/app/lib/daw/state/timeline-edit-transforms.ts` with pure helpers for timeline-edit replay (`TRACK_RENAMED`, `TRACK_OFFSET_UPDATED`, `SEGMENT_MOVED`, `SEGMENT_TRIMMED`, `SEGMENT_SPLIT`, `SEGMENT_MERGED`, `SEGMENT_FADE_SET`, `CROSSFADE_SET`) and re-exported them from `operation-reducer.ts`.
- Refactored `src/app/lib/daw/state/operation-reducer.ts` to use the new pure helpers for timeline replay cases, keeping the reducer behavior aligned with the shared transform logic.
- Added `src/app/lib/daw/state/timeline-edit-transforms.test.ts` to cover rename, move, trim, split, merge, fade, crossfade, and dispatcher behavior.
- Updated the merge assertions in `src/app/lib/daw/state/operation-reducer.test.ts` to match the canonical merged-segment shape with explicit `crossfade* = null` fields.
- Verified the touched state layer with `node --import tsx --test app/lib/daw/state/operation-reducer.test.ts` and `node --import tsx --test app/lib/daw/state/timeline-edit-transforms.test.ts` from `src/`.
- Added `src/app/lib/daw/state/timeline-edit-rebase.ts` with a pure timeline-edit rebase helper that remaps move, trim, split, merge, fade, crossfade, and rename requests against the current `LocalProjectState`.
- Reworked `packages/server/app/lib/daw/server/command-api.ts` so `commitDawProjectOperation(...)` rebases incoming timeline edits before conflict analysis and only falls back to `analyzeDawOperationConflict(...)` when the transform cannot preserve intent.
- Added `src/app/lib/daw/state/timeline-edit-rebase.test.ts` to cover rebasing move, trim, split, merge, and crossfade requests against intervening accepted operations.
- Verified the new helper and commit path with `node --import tsx --test app/lib/daw/state/timeline-edit-rebase.test.ts` and `TSX_TSCONFIG_PATH=packages/server/tsconfig.json node --import tsx --test packages/server/app/lib/daw/server/command-api.test.ts`.
- Replayed the optimistic local queue through `rebaseTimelineEditRequest(...)` inside `ProjectSyncEngine` so remote accepted operations preserve pending local timeline intent during bootstrap and realtime catch-up.
- Added `ProjectSyncEngine` coverage for an optimistic trim that is rebased after a remote accepted trim lands, alongside the existing remote accepted split regression.
- Re-ran `node --import tsx --test app/lib/daw/engine/project-sync-engine.test.ts` and `node --import tsx --test app/lib/daw/state/timeline-edit-rebase.test.ts`; both suites passed.
- Added a reducer commutativity regression in `src/app/lib/daw/state/operation-reducer.test.ts` showing two independent concurrent timeline edits converge to the same final state in either order.
- Added `packages/server/app/lib/daw/server/conflict-rules.test.ts` to lock the deterministic conflict outcome for overlapping same-track edits.
- Added a reconnect replay regression in `src/app/lib/daw/engine/project-sync-engine.test.ts` that replays a pending trim against a restored remote-accepted trim state and converges on the rebased result.
- Updated the branch checklist and the core versioning/branching feature docs under `docs/` so they now describe the landed Phase A-E behaviors: provenance-bearing versions, auto-checkpointing from accepted ops, non-destructive revert, commit-graph Tree layout, and OT-style safe concurrent edit convergence.
