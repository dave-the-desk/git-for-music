# 2026-07-14

## Implementation Log

- Continued Phase 1 of the public-repo baseline work.
- Switched the package test scripts to `node --import tsx --test` so the server suite runs without the `tsx` IPC pipe failure in this environment.
- Reworked the package lint scripts to run ESLint from the repo root, which keeps the shared flat config active for `packages/server`, `packages/shared`, and `packages/db`.
- Added the missing `@types/node` setup for `packages/shared` and tightened the config loader typing so the shared package typechecks cleanly.
- Restored the Prisma-generated client linkage after the offline relink broke the workspace, then regenerated the client so server tests can load `@prisma/client` correctly.
- Fixed the remaining server-side type drift in `command-api.ts`, `snapshot-builder.ts`, and the associated server test fixtures.
- Confirmed `pnpm --filter @git-for-music/server test` passes.
- Confirmed `pnpm --filter @git-for-music/server typecheck`, `pnpm --filter @git-for-music/shared typecheck`, and `pnpm --filter @git-for-music/db typecheck` pass.
- Confirmed `pnpm test` passes.
- Fixed the remaining `src/` typecheck failures in the DAW engine, timeline helpers, WAM host, demo client, and test fixtures.
- Confirmed `pnpm --filter @git-for-music/web test`, `pnpm test`, and `pnpm typecheck` all pass.
- Confirmed `pnpm typecheck` passes across the repo.
- Completed Phase 2 of the public-repo baseline work by finishing the typed config module coverage, migrating the remaining `process.env` call sites to `getConfig()`, adding config validation tests, and refreshing `.env.example` plus `.env.production.example`.
- Confirmed `pnpm --filter @git-for-music/server test`, `pnpm --filter @git-for-music/server typecheck`, `pnpm test`, `pnpm typecheck`, and `pnpm lint` pass, with only pre-existing lint warnings outside this change set.
- Completed Phase 3 of the public-repo baseline work by wiring the feature registry into the product surface, importing the public registrations from the root layout, and covering the registry behavior with runtime tests for registration, env overrides, duplicate-ID rejection, and downstream override.
- Confirmed `pnpm --filter @git-for-music/server test`, `pnpm --filter @git-for-music/server typecheck`, `pnpm test`, `pnpm typecheck`, and `pnpm lint` still pass after the Phase 3 changes, with the same pre-existing lint warnings outside this change set.
- Completed Phase 4 of the public-repo baseline work by adding the extension-point registry for auth, storage, analytics, and billing; routing auth/session access through the shared provider; binding the default product surface in `src/app/product/providers.ts`; and moving branding strings into `src/app/product/branding.ts`.
- Replaced the direct session-cookie lookups in the app shell, auth pages, group pages, and DAW pages with the shared auth helper, and added analytics/billing calls at the login, signup, and upload choke points.
- Added regression coverage for provider binding, session-user resolution, branding, and auth-page copy, then confirmed `pnpm --filter @git-for-music/server test`, `pnpm --filter @git-for-music/web test:unit`, `pnpm --filter @git-for-music/web test`, `pnpm typecheck`, and `pnpm lint` pass with only the pre-existing lint warnings outside this change set.
- Updated the public-repo implementation checklist to mark Phase 4 complete, and noted checklist updates in the daily log so future implementation sessions can reconcile progress against the same source of truth.
- Moved into Phase 5 of the public-repo baseline work by adding server coverage for realtime fan-out/unsubscribe behavior and same-segment/same-track conflict detection, then verified the reducer convergence suite still passes.
- Confirmed the `ProjectSyncEngine` reconnect replay coverage that rebases queued local trims onto restored remote state, while leaving the more end-to-end bootstrap reconnect variant skipped because it still reflects the current engine behavior.
- Confirmed `node --import tsx --test` passes for `packages/server/app/lib/daw/server/conflict-rules.test.ts`, `packages/server/app/lib/daw/server/realtime-gateway.test.ts`, `src/app/lib/daw/state/operation-reducer.test.ts`, and `src/app/lib/daw/engine/project-sync-engine.test.ts`.
- Added the Phase 5 Postgres+MinIO integration suite in `packages/server/app/lib/daw/server/public-repo.integration.test.ts`, covering signup, group/project creation, demo creation, track upload, snapshot bootstrap, version branching, and revert cleanup against the real database and object storage.
- Added `test:integration` scripts at the root and server package level so the integration suite is separated from the fast unit pass.
- Confirmed `pnpm --filter @git-for-music/server test:integration` and `pnpm --filter @git-for-music/server test` both pass in this workspace, with the new integration test skipped unless `RUN_INTEGRATION_TESTS=1` is set alongside the real service env vars.
- Added the Phase 6 GitHub Actions workflow in [`.github/workflows/ci.yml`](../../.github/workflows/ci.yml) with `lint`, `typecheck`, `unit`, `integration`, and `build` jobs, including Postgres/Redis/MinIO services and MinIO bucket/CORS bootstrap for the integration pass.
- Added a short CI note to [README.md](../../README.md) recommending branch protection for the new checks.
- Fixed the `src` unit-test glob in [`src/package.json`](../../src/package.json) so `app/api/versions/revert/route.test.ts` and `app/pages/api/plugins/[pluginId]/module/response-headers.test.ts` stay out of `test:unit`.
- Confirmed `pnpm --filter @git-for-music/web test:unit` passes after the glob fix.
- Diagnosed the `test:unit` failure where `app/api/versions/revert/route.test.ts` and `app/pages/api/plugins/[pluginId]/module/response-headers.test.ts` still ran and failed: `rg` is not installed on this machine, so `$(rg --files ...)` produced no files and `node --test` fell back to default discovery, which loaded the Vitest-only test files under `node:test`.
- Replaced `rg` with POSIX `find` for `node:test` file discovery in [`src/package.json`](../../src/package.json) (`test:unit`, keeping the Vitest-only exclusions) and [`packages/server/package.json`](../../packages/server/package.json) (`test`, `test:integration`), removing the ripgrep dependency for local runs and CI.
- Confirmed `pnpm --filter @git-for-music/web test:unit` (123 pass, 0 fail), `pnpm --filter @git-for-music/web test` (40 pass), and `pnpm --filter @git-for-music/server test` (74 pass) all pass after the switch.
- Rewrote the README Testing section to document the full test process: the Vitest vs `node:test` split, `find`-based discovery, the Vitest-only exclusion list, and the `pnpm test` / `test:server` / `test:unit` / `test:web` / `test:integration` commands.
- Fixed the `pnpm build` webpack failure (`Reading from "node:fs" is not handled by plugins`) caused by the shared barrel pulling `packages/shared/src/config/load.ts` into the client bundle: replaced the static `node:fs`/`node:path` imports with lazy `process.getBuiltinModule` loading that no-ops in the browser.
- Removed the `getConfig()` calls from client-side DAW code (`src/app/lib/daw/engine/project-sync-engine.ts`, `src/app/lib/daw/state/operation-reducer.ts`) in favor of `process.env.NODE_ENV !== 'production'`, since the server config loader would throw missing-secret errors in a production browser bundle.
- Relaxed the production config enforcement during `NEXT_PHASE=phase-production-build` so `next build` can collect page data without runtime secrets, while startup enforcement is unchanged; added a `config.test.ts` regression test for the build-phase behavior.
- Confirmed `pnpm build`, `pnpm test` (75 server, 123 web unit, 40 Vitest), `pnpm typecheck`, and `pnpm lint` all pass after the changes.
- Fixed snapshot replay to rehydrate durable `TrackVersion` rows when an existing version checkpoint has an empty track list, while keeping historical `operationSeq` replay isolated from current database state.
- Added a `loadSnapshotStateForDemo` regression for an empty persisted version snapshot with a track version present in the database; confirmed the full server test suite and server typecheck pass.
- Repaired the pre-deployment Prisma history with a schema-complete `00000000000000_init` migration after confirming the old chain had no initial schema migration; the old plugin visibility backfill was reviewed and not carried into the empty-database baseline.
- Isolated `NEXT_PUBLIC_APP_URL` in the config defaults test, added `db:migrate:deploy` scripts, changed Docker Compose and CI initialization to use committed migrations, and documented `db:push` as development-only.
- Validated migration deploy/status and the full signup-to-upload snapshot/branch/revert integration flow against disposable Postgres and MinIO containers on non-default ports, then destroyed those containers.
- Confirmed `pnpm test:unit`, `NEXT_PUBLIC_APP_URL=http://localhost:3000 pnpm test:unit`, server tests with and without the CI URL, `pnpm lint`, `pnpm typecheck`, and `pnpm build` pass.
- Confirmed the GitHub MinIO wait loop was caused by the service image receiving only its default `minio` command; updated CI to start MinIO with `server /data --console-address ":9001"`, use the `mc` image shell entrypoint, and fail readiness checks with diagnostics instead of looping forever.
- Completed Phase 7 of the public-repo baseline work by bumping the root and workspace package versions to `1.0.0`, adding a Releases section to the README, publishing the `v1.0.0` git tag, and creating the GitHub Release at `https://github.com/dave-the-desk/git-for-music/releases/tag/v1.0.0`.
