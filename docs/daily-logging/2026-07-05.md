# 2026-07-05

## Feature Notes Update

- Added plugin-chain context to the relevant `docs/40_FEATURES/` notes:
  - `daw-editor.md`
  - `versioning-mental-model.md`
  - `offline-sync-model.md`
  - `storage-model.md`
- Kept the notes aligned with the implemented `HostedPluginInstanceState` shape, branch/fork carry-forward behavior, reconnect replay behavior, and blob-key storage rule.

## Verification

- `git diff --check`

## Plugin Server Shapes

- Mirrored `HostedPluginInstanceState` onto the server DAW protocol and added `plugins` to the server snapshot track shapes so plugin chains can serialize through reload and version-copy flows.
- Wired the version-copy helper to carry plugin chains forward when cloning a version from snapshot state, and kept snapshot replay preserving plugin arrays when they already exist.
- Added coverage for snapshot replay, version cloning, and explicit forked-version plugin-chain preservation so the server-side serialization path stays stable.

## Verification

- `node --test --import tsx ../packages/server/app/lib/daw/server/versioning.test.ts` from `src/`
- `node --test --import tsx ../packages/server/app/lib/daw/server/snapshot-builder.test.ts` from `src/`
- `pnpm --filter @git-for-music/web test -- src/app/pages/groups/demo/components/daw/DemoDawClient.interaction.test.tsx src/app/pages/groups/demo/components/daw/VersionHistoryTree.interaction.test.tsx`

## Plugin Track State

- Added `HostedPluginInstanceState` to the DAW local project model and attached an ordered `plugins` array to `DawTrack` so plugin inserts can live on `trackVersionId` state.
- Updated the shared DAW test fixtures to seed real plugin chains by default in replay-sensitive cases, and asserted those chains survive `VERSION_BRANCH_CREATED` and `TRACK_VERSION_CREATED` replay.
- Extended the project sync engine replay fixtures to keep plugin chains intact through bootstrap reloads and accepted branch updates.
- Branch context: [plugins](../60_BRANCHES/plugins/README.md).

## Verification

- `pnpm --filter @git-for-music/web test -- src/app/lib/daw/state/operation-reducer.test.ts src/app/lib/daw/state/timeline-edit-transforms.test.ts src/app/lib/daw/state/timeline-edit-rebase.test.ts src/app/lib/daw/engine/project-sync-engine.test.ts`

## Current DAW UI Explanation

- Added a live snapshot of the current DAW visual system to `docs/30_UI/` so the vault now has a descriptive explanation for how the UI actually looks today.
- Linked the new note from the UI routing note for easier navigation.

## Verification

- Docs-only update; no code test run required.

## Browser Upload Polish

- Removed the browser rail's filter search row so the left card focuses on the actual upload workflow.
- Reworked the upload form with a clearer title, helper text, a cleaner file-picker card, and a stronger upload button.

## Verification

- `pnpm --filter @git-for-music/web test -- src/app/pages/groups/demo/components/daw/DemoDawClient.interaction.test.tsx src/app/pages/groups/demo/components/daw/VersionHistoryTree.interaction.test.tsx`

## Version Tree Header Cleanup

- Removed the inner version-tree collapse toggle so the only collapse control left is the rail button in the outer header.
- Kept the color key and action buttons at the top of the tree body so they sit directly under the Version History title.

## Verification

- `pnpm --filter @git-for-music/web test -- src/app/pages/groups/demo/components/daw/VersionHistoryTree.interaction.test.tsx src/app/pages/groups/demo/components/daw/DemoDawClient.interaction.test.tsx`

## Version Tree Down Nudge

- Nudged the version-tree rail a little lower from the inspector body height so it lines up closer to the lower line below the Post comment button.
- Kept the fixed-height rail and internal scrolling behavior unchanged.

## Verification

- `pnpm --filter @git-for-music/web test -- src/app/pages/groups/demo/components/daw/VersionHistoryTree.interaction.test.tsx src/app/pages/groups/demo/components/daw/DemoDawClient.interaction.test.tsx`

## Version Tree Inspector Match

- Switched the version-tree height measurement from the comment card to the full inspector scroll container so the rail can line up with the exact element boundary you highlighted.
- Kept the tree content scrollable inside the fixed-height rail so the DAG still uses overflow instead of extending past the target line.

## Verification

- `pnpm --filter @git-for-music/web test -- src/app/pages/groups/demo/components/daw/VersionHistoryTree.interaction.test.tsx src/app/pages/groups/demo/components/daw/DemoDawClient.interaction.test.tsx`

## Version Tree Height Pullback

- Backed out the extra chrome-based expansion so the version tree rail now stops at the measured comment-card height again, which brings the graph cutoff back up to the requested boundary.
- Kept the tree body scrollable inside the rail so the fixed-height viewport still scrolls instead of overflowing.

## Verification

- `pnpm --filter @git-for-music/web test -- src/app/pages/groups/demo/components/daw/VersionHistoryTree.interaction.test.tsx src/app/pages/groups/demo/components/daw/DemoDawClient.interaction.test.tsx`

## Version Tree Snap Back

- Removed the extra offset from the version-history rail so its bottom edge now snaps back up to the measured comment-card height in the inspector.
- Left the tree's internal scrolling behavior unchanged, so the rail still scrolls instead of growing beyond its boundary.

## Verification

- `pnpm --filter @git-for-music/web test -- src/app/pages/groups/demo/components/daw/VersionHistoryTree.interaction.test.tsx src/app/pages/groups/demo/components/daw/DemoDawClient.interaction.test.tsx`

## Version Tree Outer Rail Extension

- Moved the extra height from the inner graph viewport to the outer version-history rail so the whole section now reaches lower on the page.
- Kept the internal scroll viewport in place, so the content still scrolls instead of forcing the layout to grow.

## Verification

- `pnpm --filter @git-for-music/web test -- src/app/pages/groups/demo/components/daw/VersionHistoryTree.interaction.test.tsx src/app/pages/groups/demo/components/daw/DemoDawClient.interaction.test.tsx`

## Version Tree Viewport Flex Fix

- Converted the version tree body into a true flex column so the graph viewport now stretches to fill the measured height instead of sizing to its content.
- Kept the measured fixed-height rail in place so the tree still bottoms out relative to the comment card while actually using that space.

## Verification

- `pnpm --filter @git-for-music/web test -- src/app/pages/groups/demo/components/daw/VersionHistoryTree.interaction.test.tsx src/app/pages/groups/demo/components/daw/DemoDawClient.interaction.test.tsx`

## Version Tree Viewport Extension

- Pushed the version tree viewport further downward so the graph body now reaches much closer to the comment card's bottom edge on the right.
- Kept the fixed-height measured viewport and internal scrolling behavior in place, so the extra depth does not change interaction handling.

## Verification

- `pnpm --filter @git-for-music/web test -- src/app/pages/groups/demo/components/daw/VersionHistoryTree.interaction.test.tsx src/app/pages/groups/demo/components/daw/DemoDawClient.interaction.test.tsx`

## Version Tree Viewport Nudge

- Extended the version tree viewport a little farther down than the comment card boundary so the visible tree area sits lower in the rail.
- Kept the inner scroll container intact so the added height still scrolls rather than pushing the rest of the layout.

## Verification

- `pnpm --filter @git-for-music/web test -- src/app/pages/groups/demo/components/daw/VersionHistoryTree.interaction.test.tsx src/app/pages/groups/demo/components/daw/DemoDawClient.interaction.test.tsx`

## Version Tree Button Emphasis

- Moved the version-tree action buttons into a left-aligned control row so they read as primary actions instead of floating utility pills.
- Increased the visual weight of checkout, branch, and revert actions with stronger fill, spacing, and shadow treatment.

## Verification

- `pnpm --filter @git-for-music/web test -- src/app/pages/groups/demo/components/daw/VersionHistoryTree.interaction.test.tsx src/app/pages/groups/demo/components/daw/DemoDawClient.interaction.test.tsx`

## Version Tree Height Match

- Changed the version history rail from a loose height clamp to an explicit measured height so it matches the comment card boundary on the right.
- Kept the tree body scrollable inside that fixed viewport so the rail stops exactly at the same vertical point as the comment card.

## Verification

- `pnpm --filter @git-for-music/web test -- src/app/pages/groups/demo/components/daw/VersionHistoryTree.interaction.test.tsx src/app/pages/groups/demo/components/daw/DemoDawClient.interaction.test.tsx`

## Version Tree Cleanup

- Removed the explanatory subtext from the version-history tree so the rail reads as a cleaner, denser control surface.
- Simplified the tree header so the section relies on the outer rail title and compact expand/collapse controls instead of repeating labels.

## Verification

- `pnpm --filter @git-for-music/web test -- src/app/pages/groups/demo/components/daw/VersionHistoryTree.interaction.test.tsx src/app/pages/groups/demo/components/daw/DemoDawClient.interaction.test.tsx`

## Version Tree Scroll Containment

- Bound the version history rail to the measured height of the comment card and moved the overflow handling onto the tree body itself.
- Kept horizontal scrolling available only inside the tree container when the DAG is wider than the card.

## Verification

- `pnpm --filter @git-for-music/web test -- src/app/pages/groups/demo/components/daw/DemoDawClient.interaction.test.tsx`

## Version Tree Scroll Bound

- Constrained the version history rail so it stays within the height of the comment card region instead of extending past it.
- Added internal scroll handling for the tree rail so overflow now scrolls vertically, and horizontally only when the graph exceeds the card width.

## Verification

- `pnpm --filter @git-for-music/web test -- src/app/pages/groups/demo/components/daw/DemoDawClient.interaction.test.tsx`

## DAW Top Shell Pinning

- Kept the demo title, timing controls, and recording controls pinned as the always-on-top shell above the rest of the DAW workspace.
- Removed the wrapper overflow constraint that could interfere with sticky positioning so the top card remains fixed in the page flow as intended.

## Verification

- `pnpm --filter @git-for-music/web test -- src/app/pages/groups/demo/components/daw/DemoDawClient.interaction.test.tsx`

## DAW Shell Visual Polish

- Refreshed the demo DAW shell visuals with layered background treatment, calmer rail surfaces, and tighter top-bar hierarchy while keeping the feature set unchanged.
- Updated the alignment plan to remove the retired detail/edit and happy-accident phases so the roadmap matches the current page.

## Verification

- `pnpm --filter @git-for-music/web test -- src/app/pages/groups/demo/components/daw/DemoDawClient.interaction.test.tsx`

## Detail Pane Rollback

- Removed the standalone `DetailEditPane` implementation and the timeline-mounted detail/edit surface from the demo DAW.
- Dropped the corresponding interaction assertions and removed the generated inventory entry so the docs no longer describe that pane as part of the app.

## Verification

- Pending rerun of the focused DAW interaction slice after the rollback.

## Track Delete Realtime

- Added a trash-can delete button to each track row so tracks can be removed directly from the canvas.
- Routed track deletion through the DAW realtime commit path so the remove operation propagates through sync, reducer, snapshot, and conflict handling instead of staying local.
- Extended the interaction regression to prove the delete request updates the shared project state and clears the track from the rendered workspace.

## Verification

- `pnpm --filter @git-for-music/web test -- src/app/pages/groups/demo/components/daw/DemoDawClient.interaction.test.tsx`

## DAW Chrome Trim

- Removed the helper text under the version-history header so the version rail now reads more tersely.
- Trimmed the inspector rail down to comments only by removing the selection and clip detail blocks from that panel.

## Verification

- `pnpm --filter @git-for-music/web test -- src/app/pages/groups/demo/components/daw/DemoDawClient.interaction.test.tsx`
- `git diff --check`

## DAW Phase 5 Detail Pane

- Added a new non-modal `DetailEditPane` below the timeline so the DAW now has a dedicated waveform editor surface in the center column.
- Wired the pane to the selected track and selected clip state so it can show waveform preview data, segment details, and a direct jump-to-clip action.
- Stubbed the remaining representation tabs as disabled coming-soon tabs for piano roll, notation, automation, and spectral views.
- Added interaction coverage for the new pane and its accessible tab labels, plus a lightweight waveform mock for the browser test harness.

## Verification

- `pnpm --filter @git-for-music/web test -- src/app/pages/groups/demo/components/daw/DemoDawClient.interaction.test.tsx`
- `git diff --check`

## Project Timing Layout Fix

- Reworked the project timing card so shared demo tempo and local tempo sit side by side on wide screens instead of stacking vertically.
- Kept the mobile layout stacked so the card still compresses cleanly on narrow viewports.

## Verification

- `pnpm --dir src test -- src/app/pages/groups/demo/components/daw/DemoDawClient.interaction.test.tsx`
- `git diff --check`

## Tools Stack Fix

- Added a sticky-state spacer behind the tools bar so it holds its own vertical space when pinned and no longer overlaps the timeline while scrolling.
- Kept the toolbar sticky behavior intact, but made the timeline start below the pinned tools instead of sliding underneath it.

## Verification

- `pnpm --dir src test -- src/app/pages/groups/demo/components/daw/DemoDawClient.interaction.test.tsx`
- `git diff --check`

## Microphone Panel Fix

- Changed the microphone selector from an overlapping popover into an in-flow panel so the actual input list stays reachable inside the recording block.
- Kept the selector attached to the recording controls, but removed the layout overlap that was blocking the rest of the recording surface.

## Verification

- `pnpm --dir src test -- src/app/pages/groups/demo/components/daw/DemoDawClient.interaction.test.tsx`
- `git diff --check`

## Recording Box Layout Fix

- Reflowed the recording block into a tighter two-column layout so the microphone selector sits in its own narrow rail instead of stretching into an oversized full-width row.
- Grouped the record, timer, and stop controls into the right-hand action area so the block reads as a compact control surface.

## Verification

- `pnpm --dir src test -- src/app/pages/groups/demo/components/daw/DemoDawClient.interaction.test.tsx`
- `git diff --check`

## DAW Top Block Tightening

- Reduced the padding, gaps, and control sizing in the merged timing and recording block so it reads as a compact top bar instead of two separate oversized cards.
- Kept the microphone chooser embedded inside the recording controls while shrinking the surrounding spacing and action buttons.
- Removed the legacy version-history toolbar strip at the bottom of the rail so the version history panel now ends cleanly.

## Verification

- `pnpm --dir src test -- src/app/pages/groups/demo/components/daw/DemoDawClient.interaction.test.tsx`
- `git diff --check`

## DAW Control Group Merge

- Moved the microphone chooser into the recording block so the recording controls now own the full input/arming surface.
- Added a minimize/restore control for the combined project timing and recording row so the top control group can be collapsed without leaving the page.
- Merged the Tools strip and timeline into one left-aligned block so the tool buttons sit on the left side of the page above the timeline.

## Verification

- `pnpm --dir src test -- src/app/pages/groups/demo/components/daw/DemoDawClient.interaction.test.tsx`
- `git diff --check`

## DAW Layout Reflow Fix

- Removed the page-level zoom shrink and kept the demo shell at normal scale.
- Moved the responsive breakpoint for the top control row and the main browser/canvas/inspector grid down to `lg` so the desktop layout stays horizontal at normal zoom.
- Added a measured sticky Tools strip directly above the timeline so it stays pinned beneath the timing and recording card when the page scrolls.

## Project Timing Cleanup

- Removed the remaining subtext from the Project Timing card so it now reads as a compact control surface instead of a help panel.

## Verification

- Pending rerun of the focused DAW interaction slice after the breakpoint change.

## DAW UI Shrink and Copy Cleanup

- Removed the temporary page-level zoom shrink and kept the shell at normal scale.
- Removed explanatory subtext across the browser, version-history, inspector, and comment composer surfaces so the page now reads as controls and content instead of guide copy.
- Kept the browser search, inspector access, and comment composer reachable through accessibility labels while tightening the visible chrome.

## Verification

- `pnpm --dir src test -- src/app/pages/groups/demo/components/daw/version-history-tree.test.ts src/app/pages/groups/demo/components/daw/version-graph-layout.test.ts src/app/pages/groups/demo/components/daw/VersionHistoryTree.interaction.test.tsx src/app/pages/groups/demo/components/daw/DemoDawClient.interaction.test.tsx`
- `pnpm --dir src test -- src/app/pages/groups/demo/components/daw/DemoDawClient.interaction.test.tsx`

## DAW Inspector Phase 4

- Added a dedicated right inspector rail beside the timeline so the selected track, clip details, and comment composer live in one persistent context panel.
- Mirrored record-arm, mute, solo, and volume controls into the inspector so the selected track can be edited without hunting through the canvas.
- Replaced the old comment modal with an inline inspector composer and added a regression that exercises the new rail and its comment priming flow.

## Verification

- `pnpm test -- src/app/pages/groups/demo/components/daw/version-history-tree.test.ts src/app/pages/groups/demo/components/daw/version-graph-layout.test.ts src/app/pages/groups/demo/components/daw/VersionHistoryTree.interaction.test.tsx src/app/pages/groups/demo/components/daw/DemoDawClient.interaction.test.tsx`
- `pnpm test -- src/app/pages/groups/demo/components/daw/DemoDawClient.interaction.test.tsx`

## DAW Browser Phase 3

- Moved the upload, plugins, and members surfaces into a dedicated left browser rail so the workspace now has a clearer browser/canvas split.
- Added browser search and tab controls inside the rail, with the upload form, plugin list, and member presence states all rendered from that single panel.
- Added a browser-rail interaction regression so the moved controls are exercised in place and the empty plugin/member states remain reachable.

## Verification

- `pnpm test -- src/app/pages/groups/demo/components/daw/DemoDawClient.interaction.test.tsx`
- `pnpm test -- src/app/pages/groups/demo/components/daw/version-history-tree.test.ts src/app/pages/groups/demo/components/daw/version-graph-layout.test.ts src/app/pages/groups/demo/components/daw/VersionHistoryTree.interaction.test.tsx src/app/pages/groups/demo/components/daw/DemoDawClient.interaction.test.tsx`

## Top Edge Flush Fix

- Removed the top inset above the sticky DAW header so the demo timing and transport controls now sit flush against the top edge of the screen.
- Kept the header sticky behavior and existing workspace spacing below it intact.

## Verification

- Pending rerun of the DAW interaction slice after the layout adjustment.

## DAW Top Bar Phase 2

- Moved `ProjectTimingControls`, `TransportControls`, `RecordingControls`, the metronome toggle, and `AudioInputSelector` into a single sticky top bar above the demo workspace.
- Removed the separate right-side control rail so the page now reads as a single top bar plus the main editing surface beneath it.
- Added a viewport-width listener so the docked version rail can react when the window crosses the desktop breakpoint instead of only deciding once at mount.
- Added a browser regression that verifies the top-bar controls render, and a resize regression that checks the version rail switches between expanded and collapsed states as the window width changes.

## Verification

- `pnpm test -- src/app/pages/groups/demo/components/daw/version-history-tree.test.ts src/app/pages/groups/demo/components/daw/version-graph-layout.test.ts src/app/pages/groups/demo/components/daw/VersionHistoryTree.interaction.test.tsx src/app/pages/groups/demo/components/daw/DemoDawClient.interaction.test.tsx`

## DAW Version Rail Phase 1

- Promoted the demo DAW version history into a persistent rail on the demo screen instead of keeping it hidden behind the toolbar tab.
- Added per-project/per-user rail expand state persisted to `localStorage`, with a desktop-friendly default and a tab-driven focus/expand path for the legacy tree entry point.
- Added a docked-rail expansion mode to `VersionHistoryTree` so the graph can render open by default when it is mounted as a rail.
- Updated the demo interaction regression to prove the rail and tree mount on initial render, and added a rail-mode interaction test for the expanded tree component.

## Verification

- `pnpm test -- src/app/pages/groups/demo/components/daw/version-history-tree.test.ts src/app/pages/groups/demo/components/daw/version-graph-layout.test.ts src/app/pages/groups/demo/components/daw/VersionHistoryTree.interaction.test.tsx src/app/pages/groups/demo/components/daw/DemoDawClient.interaction.test.tsx`

## Implementation Log

- Added a browser-level interaction regression test for the full `record on Track 1 -> stop/save -> add track` flow in `DemoDawClient`.
- Reproduced a real failure in that flow: the recording save path could throw while calling `scrollTo` on the tracks container in jsdom-like environments, which interrupted cleanup and left the temporary recording lane visible.
- Guarded the `scrollTo` call so recording save can finish cleanly even when the container does not implement that method.
- Kept the existing recording cleanup behavior intact so the committed track replaces the preview lane before the next add-track action.

## Verification

- `pnpm --filter @git-for-music/web exec vitest run app/pages/groups/demo/components/daw/DemoDawClient.interaction.test.tsx`
- `pnpm --filter @git-for-music/web exec vitest run app/pages/groups/demo/components/daw/VersionHistoryTree.interaction.test.tsx app/pages/groups/demo/components/daw/DemoDawClient.interaction.test.tsx`
- `git diff --check`

## Add Track Race Fix

- Added `resolveUploadSourceVersionId` so uploads prefer the freshly committed version when the active version sync is still lagging behind.
- Wired `DemoDawClient` add-track uploads through that helper instead of always forking from the stale live active version.
- Added a unit regression test for the helper and a browser interaction regression test that keeps `setActiveVersion` pending long enough to reproduce the race.
- Verified the focused helper and interaction tests pass with the new source-version selection.

## Verification

- `pnpm --filter @git-for-music/web test -- src/app/lib/daw/utils/upload-source-version.test.ts src/app/pages/groups/demo/components/daw/DemoDawClient.interaction.test.tsx`

## Collaborator Head Follow Fix

- Changed the demo DAW client to follow the shared branch head whenever the live project state changes, instead of waiting for the local active-version field to move first.
- Added a regression test that simulates a collaborator creating the next version from a blank DAW while the local client stays on the old active version, and verifies the rendered track list updates without a refresh.

## Verification

- `pnpm --filter @git-for-music/web test -- src/app/pages/groups/demo/components/daw/DemoDawClient.interaction.test.tsx`

## Two-Client Collaboration Regression

- Added a second browser interaction regression that mounts two DAW clients simultaneously against the same project state.
- Verified that when one client adds a track to a blank demo, both clients render the new track without a refresh.

## Verification

- `pnpm --filter @git-for-music/web test -- src/app/pages/groups/demo/components/daw/DemoDawClient.interaction.test.tsx`

## Realtime First-Open Recovery

- Added a realtime silence watchdog to `ProjectSyncEngine` so a stalled DAW `EventSource` now forces a reconnect instead of waiting for a manual refresh.
- Made the engine run a catch-up request as soon as the realtime stream opens so existing operations are applied immediately on first load.
- Added a regression test that simulates an open stream, verifies the catch-up fetch, and confirms the watchdog opens a new realtime connection after silence.

## Verification

- `pnpm --filter @git-for-music/web test -- src/app/lib/daw/engine/project-sync-engine.test.ts`

## Track Add Follow-Head Fix

- Updated the reducer so `TRACK_VERSION_CREATED` now advances the shared branch head and the follow-head checkout when the new version is marked current.
- This keeps newly created demo additions visible to collaborators immediately instead of leaving them on the stale pre-upload version until refresh.
- Added a reducer regression test that proves a track creation op moves both `currentVersionId` and `activeVersionId` to the new version for follow-head clients.

## Verification

- `pnpm --filter @git-for-music/web test -- src/app/lib/daw/state/operation-reducer.test.ts`
- `pnpm --filter @git-for-music/web test -- src/app/lib/daw/engine/project-sync-engine.test.ts`

## Blank Demo Track Sync

- Extended the `TRACK_VERSION_CREATED` realtime payload so the server now sends the created version node alongside the new track.
- Applied the same payload shape to both upload entrypoints, covering blank-demo track creation and the full upload asset flow.
- Updated the reducer so a follow-head client advances to the new version head as soon as the track-create event arrives, instead of waiting for a refresh.

## Verification

- `pnpm --filter @git-for-music/web test -- src/app/lib/daw/state/operation-reducer.test.ts src/app/lib/daw/engine/project-sync-engine.test.ts`
- `pnpm --filter @git-for-music/server exec tsc -p tsconfig.json --noEmit --pretty false 2>&1 | rg 'complete-upload\\.ts|upload-track\\.ts|protocol/command-api\\.ts|TRACK_VERSION_CREATED'`

## Realtime Cut Sync

- Added a lightweight periodic catch-up loop to `ProjectSyncEngine` so an open DAW session re-pulls accepted operations even if the live stream stays quiet or stalls.
- Kept the existing silence watchdog in place so a dead stream still forces a reconnect, while the new poller keeps the state current between reconnects.
- Extended the realtime regression test to prove an opened engine both catches up immediately and then re-syncs on the next scheduled poll before reconnecting on silence.

## Verification

- `pnpm --filter @git-for-music/web test -- src/app/lib/daw/engine/project-sync-engine.test.ts`

## Duplicate Track Cleanup

- Added a shared duplicate-cleanup helper that prunes blank track versions when a track is added with the same `trackId` or same `trackName`.
- Wired the cleanup into the client reducer bootstrap/replay path so stale blank copies are removed as soon as project state is normalized or a `TRACK_VERSION_CREATED` operation lands.
- Wired the same cleanup into both server upload flows so add-track and recording uploads delete the blank duplicate server-side before the operation is committed.
- Added reducer and server helper regression tests that prove same-name audio tracks are preserved while blank duplicates are removed.

## Verification

- `node --import tsx --test app/lib/daw/utils/track-duplicate-cleanup.test.ts app/lib/daw/state/operation-reducer.test.ts app/lib/daw/utils/upload-source-version.test.ts` from `src/`
- `node --import tsx --test app/lib/daw/server/track-duplicate-cleanup.test.ts` from `packages/server/`
- `pnpm --filter @git-for-music/web exec vitest run app/pages/groups/demo/components/daw/DemoDawClient.interaction.test.tsx`

## Fresh Commit Source Fix

- Replaced the version-selection logic for uploads with a freshest-committed-version ref, which avoids depending on a still-stale render state when a recording has just been saved.
- Updated the regression test so it waits for the recording save to finish, then clicks **+ Add track** while the active-version sync is still pending. That matches the duplicate-`Track 1` window more closely.
- Verified the helper test and browser interaction test both pass with the ref-based source selection.

## Verification

- `pnpm --filter @git-for-music/web test -- src/app/lib/daw/utils/upload-source-version.test.ts src/app/pages/groups/demo/components/daw/DemoDawClient.interaction.test.tsx`

## Follow-up Race Guard

- Tightened the add-track regression test so it clicks **+ Add track** while the recording save is still pending, which is the window that was still reproducing the duplicate-track behavior.
- Added an explicit `isSavingRecording` guard in `DemoDawClient` so add-track is disabled and ignored until the recording commit finishes.
- Re-ran the focused helper and interaction tests after the guard; they now pass with the in-flight recording window covered.

## Verification

- `pnpm --filter @git-for-music/web test -- src/app/lib/daw/utils/upload-source-version.test.ts src/app/pages/groups/demo/components/daw/DemoDawClient.interaction.test.tsx`
